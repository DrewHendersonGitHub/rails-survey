<h1>Surveys</h1>

<% if @surveys.any? %>
  <ul>
    <% @surveys.each do |survey| %>
      <li><%= link_to survey.name, survey_path(survey) %></li>
    <% end %>
  </ul>
<% else %>
  <p>There aren't any surveys.</p>
<% end %>


<p><%= link_to "Add new survey", new_survey_path %></p>